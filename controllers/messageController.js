const db = require("../models");

// Defining methods for the booksController
module.exports = {
    getMessages: (req,res) => {

    },
    sendMessage: (req,res) => {

    },
    deleteMessage: (req,res) => {
    	
    }
}